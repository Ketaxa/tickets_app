import SearchScreen from './SearchScreen'
import NotificationScreen from './NotificationScreen'
const Screens = {
    SearchScreen: Object.assign(SearchScreen, SearchScreen),
NotificationScreen: Object.assign(NotificationScreen, NotificationScreen),
}

export default Screens
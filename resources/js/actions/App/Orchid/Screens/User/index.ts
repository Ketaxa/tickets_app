import UserProfileScreen from './UserProfileScreen'
import UserEditScreen from './UserEditScreen'
import UserListScreen from './UserListScreen'
const User = {
    UserProfileScreen: Object.assign(UserProfileScreen, UserProfileScreen),
UserEditScreen: Object.assign(UserEditScreen, UserEditScreen),
UserListScreen: Object.assign(UserListScreen, UserListScreen),
}

export default User